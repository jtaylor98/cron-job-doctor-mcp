name: Heartbeat

on:
  schedule:
    - cron: "*/15 * * * *"  # every 15 minutes
  workflow_dispatch:         # lets you trigger it manually too, for instant testing

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Still alive at $(date -u)"
