# Fine-grained GitHub Personal Access Token.
# Scopes needed: Actions (read-only), Contents (read-only), Metadata (read-only)
# Create one at: https://github.com/settings/personal-access-tokens/new
GITHUB_TOKEN=
